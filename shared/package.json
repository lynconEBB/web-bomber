{
  "name": "@web-bomber/shared",
  "version": "1.0.0",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "rimraf build"
  },
  "files": [
    "build"
  ],
  "dependencies": {
    "@colyseus/schema": "^3.0.76"
  },
  "devDependencies": {
    "typescript": "^5.0.4",
    "rimraf": "^5.0.0"
  }
}